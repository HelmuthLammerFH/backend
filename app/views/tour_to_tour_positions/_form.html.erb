<%= form_for(tour_to_tour_position) do |f| %>
  <% if tour_to_tour_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tour_to_tour_position.errors.count, "error") %> prohibited this tour_to_tour_position from being saved:</h2>

      <ul>
      <% tour_to_tour_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tourPosition_id %>
    <%= f.number_field :tourPosition_id %>
  </div>

  <div class="field">
    <%= f.label :startDate %>
    <%= f.datetime_select :startDate %>
  </div>

  <div class="field">
    <%= f.label :endDate %>
    <%= f.datetime_select :endDate %>
  </div>

  <div class="field">
    <%= f.label :createdFrom %>
    <%= f.text_field :createdFrom %>
  </div>

  <div class="field">
    <%= f.label :changedFrom %>
    <%= f.text_field :changedFrom %>
  </div>

  <div class="field">
    <%= f.label :syncedFrom %>
    <%= f.number_field :syncedFrom %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
